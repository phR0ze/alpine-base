name: Publish Docker Image
on:
  release:
    types: [published]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
    - name: Extract the release tag
      id: release
      run: ::set-output name=tag::$(basename "${GITHUB_REF}")
      #- name: Build Docker image
      # run: docker build . --file Dockerfile --tag alpine-base:${{ steps.release.outputs.tag }}
